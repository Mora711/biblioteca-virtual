public class Url {
    String url;
    Titulo titulo;

    //ctrl + insert -> con eso aumentamos
    public Url(String url, Titulo titulo) {
        this.url = url;
        this.titulo =titulo;
    }

    public Url(String url) {
        this.url = url;
    }
}
